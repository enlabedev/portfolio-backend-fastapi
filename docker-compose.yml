services:
  backend:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/.venv
    environment:
      - WATCHFILES_FORCE_POLLING=true
    command: uvicorn app.main:app --host 0.0.0.0 --port 8080 --reload
